import ComingSoon from '@/components/ComingSoon';
import { Archive } from 'lucide-react';

export default function ClosingPage() {
  return <ComingSoon title="Závierka & podanie" subtitle="Účtovná závierka pre RÚZ" icon={<Archive size={24} />} desc="Modul závierky je v príprave." />;
}
