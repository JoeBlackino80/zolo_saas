import ComingSoon from '@/components/ComingSoon';
import { Target } from 'lucide-react';

export default function OptimizePage() {
  return <ComingSoon title="Optimalizácia DPH" subtitle="Skupinové započítanie · inter-firm netting · what-if scenáre" icon={<Target size={24} />} desc="Modul optimalizácie je v príprave." />;
}
